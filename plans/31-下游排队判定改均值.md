# 计划 31：下游排队判定改均值

## 改动

1. `app/data/load_intersection_from_pg.py::_aggregate_metrics`
   - `queue_m` = mean(`queue_len_avg`)；无 avg 则 mean(`queue_len_max`)
   - 新增 `queue_m_max` = max(max, avg) 供展示/长排队旗标
2. `app/data/pg_adapters.py::metrics_for_diagnosis`
   - 从 `turn_perf_detail` 按 dir8+turn 聚合 mean(avg)，驱动 `queue_length_m`
3. 单测：`tests/test_queue_avg_judgment.py`
4. bugs：`bugs/BUG-006-downstream-queue-used-max.md`
