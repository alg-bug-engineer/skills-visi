# 计划 27 · 语音认知、地图动作、经验对比与案例相似度

> 需求：`needs/27-语音地图经验案例迭代.md`  
> 分支：`20260710101225-27-语音地图经验案例迭代`

## 范围（本分支）

| 条目 | 状态 | 说明 |
|------|------|------|
| **R1** 路口认知语音 | ✅ 本分支 | `axis_roads` 真源 + act2 TTS |
| **R4** 案例相似度细化 | ✅ 本分支 | 后端契约扩展 + CauseCard UI |
| **R2** 全链路地图动作 | 📋 后续 | 分 act 批次，复用 trace/channelization 图层 |
| **R3** 有经验 vs 无经验 | 📋 后续 | 依赖 R4 与经验库结构化 |
| **R5** 轨迹对接亚楠 | ⏸ 不开发 | 对接清单保留在 needs |

## R1 设计

1. `app/trace/axis_roads.py`：从渠化 entrance `road_name` / `dir8` 推导 `ew_road`、`ns_road`；缺失时从 `intersection_name` 解析两路名（不猜 EW/NS 方位）。
2. `build_spatial_scene.py`：写入 `axis_roads`（name 级 fallback）。
3. `response_builder.py`：诊断完成后用 `channelization_map.links` 覆盖 enrichment。
4. 前端 `voiceSpatialCognition` + `act2_locate` 播报前缀；缺失半轴降级文案。

## R4 设计

1. `CaseLibraryService.search_case_cards` 扩展：
   - `similarity_dimensions[]`（场景/指标/措施）
   - `transferable_actions[]`、`caveats[]`、`help_summary`
   - `similarity_tier`：`high` | `matched`
2. `CauseCard.vue`：结构化展示 + tooltip 分级说明。
3. 单测：`test_axis_roads.py`、`test_case_library_readonly` 扩展、前端 voice/cause 测试。

## 测试

```bash
PYTHONPATH=. .venv/bin/python -m pytest tests/test_axis_roads.py tests/test_case_library_readonly.py -q
cd frontend && npm test -- voice-spatial-cognition cause-card-cases voice-templates
```
