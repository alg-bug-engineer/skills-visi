# 流量溯源干线 geom 重构 · 交付复盘

**日期：** 2026-06-30  
**演示路口：** `011wwe28ctu00001`（奥体西路×经十路）  
**关联计划：** [2026-06-30-流量溯源干线geom重构计划.md](./2026-06-30-流量溯源干线geom重构计划.md)、[2026-06-30-单链路发光溯源重构计划.md](./2026-06-30-单链路发光溯源重构计划.md)

---

## 1. 问题与根因

| 用户反馈 | 根因 |
|----------|------|
| 西左转方向反了 | `flow_correlate` 按 `flow_share_ratio` 取最大，未校验 link 邻接/方位角 |
| 两点飞线 | storyboard `edge.path` 为空时前端两点 fallback |
| 饱和度重复 | 分组标 + `enrichSaturationHints` + 排队标签叠加 |
| 红色遮罩 | 溯源阶段 `drawHighlights()` 重绘诊断框 |

**嗅探结论（西进口）：** link 拓扑上一跳 = **经十路与转山西路路口**；旧 correlate 一跳误指东侧「奥体中路与经十路」。

---

## 2. 方案（三层）

1. **拓扑一跳（主）** — `dim_link_info.geom` + 进口宽表 `dwd_tfc_rltn_wide_inter_ft_link`
2. **correlate 佐证** — 白名单邻接路口内取流量占比
3. **单链治理溯源** — 沿 `corridor_dir8` 递归 ≤5 跳，产出 storyboard

实现入口：`UpstreamTopologyService`、`UpstreamGovernanceTraceService`、`upstreamTraceLayer.ts`。

---

## 3. 交付清单

### 后端

- [x] `upstream_topology_service.py`：`pick_upstream_hop`、方位角容差、`orient_path_upstream_to_target`
- [x] `upstream_governance_trace_service.py`：单链 `build_tree`，`hop_path` / `path_source`
- [x] `map_presentation_service.py`：edge.path 来自节点 `hop_path`
- [x] `orchestrator.py`：西左转等只溯单进口；mock 可治理落点
- [x] `probe_upstream_topology.py` 嗅探脚本
- [x] 测试：`test_upstream_topology.py`、`test_build_tree.py`、`test_upstream_storyboard.py`、`test_orchestrator_upstream_phase.py`

### 前端

- [x] `upstreamTraceLayer.ts`：发光干线 + 粒子 + 节点脉冲 + 极简标签
- [x] `MapStage.vue`：溯源期禁红遮罩；`resolveEdgePath` 禁飞线
- [x] `buildRoleArmLabels`：饱和 · 失衡 · 排队 一行合并
- [x] 删除旧 `upstreamGradient.ts`

### 回归与 E2E

- [x] `REGRESSION_TEST_SPEC.md`：RT-TRACE-07/08/09
- [x] `scripts/verify-upstream-trace.mjs`（`LABEL=westleft` 验收转山西路标签）

### 文档

- [x] `docs/流量溯源表结构信息.md` §应用层拓扑 vs 统计途经
- [x] `backend/docs/PROJECT_LOGIC.md` §8 进口道流量溯源

---

## 4. 手验清单

```bash
# 嗅探
cd backend && .venv/bin/python scripts/probe_upstream_topology.py --inter 011wwe28ctu00001 --dir8 6 --turn 1

# 回归
bash scripts/regression.sh

# E2E 截图（需 dev-v2 已启动）
cd frontend-v2 && LABEL=westleft PROMPT='奥体西路与经十路交叉口，下午四点西左转排队过长' node ../scripts/verify-upstream-trace.mjs
```

**通过标准：**

1. 西左转：地图标签含「转山西路」，单链向西
2. 无 `.us-card` / 红色遮罩 / 飞线 fallback
3. 关注进口臂标一行：`饱和 · 失衡 · 排队`（不重复）

---

## 5. 已知限制

- Playwright headless 下 `.us-particle` 偶发为 0，E2E 降为 P1 软检查（RT-TRACE-09）；实机发光干线正常。
- `flow_trace_service.build_entry_traces` 仍保留 correlate 逻辑，与地图拓扑链分工不同（列表摘要 vs 运镜链）。

---

## 6. 测试规模（交付时）

- 后端 pytest：**272**
- 前端 vitest：**211**
