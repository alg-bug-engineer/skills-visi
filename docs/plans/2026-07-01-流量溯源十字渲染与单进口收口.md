# 流量溯源：十字渲染与单进口收口

**日期：** 2026-07-01  
**状态：** 实施中  
**规则索引：** [`docs/UPSTREAM_TRACE_RULES.md`](../UPSTREAM_TRACE_RULES.md)

---

## 目标

1. 文档与代码对齐：废弃二跳三分叉、多进口并行等旧设计。  
2. 单进口道：方向组默认第一方位（东西向→东进口）。  
3. 上游路口十字高亮：`turn_split` → `feed_segments` 道路段渲染。  
4. 递归上限保持 **5 跳**。

---

## 任务清单

| 阶段 | 内容 | 状态 |
|------|------|------|
| D0 | `UPSTREAM_TRACE_RULES.md` + 废弃旧计划文档 | ✅ |
| P1 | `trace_approach.resolve_trace_approach` + orchestrator 收口 | ✅ |
| P2 | `resolve_feed_segments` + storyboard 节点/reveal | ✅ |
| P3 | `UpstreamTraceLayer.revealFeedSegment` + MapStage | ✅ |
| P4 | 单测 + `bash scripts/regression.sh` | ✅ |

---

## 验收

- `东西向` 仅溯东进口（RT-TRACE-10）  
- hop1 节点含 `feed_segments`，path 来自 link_geom  
- 地图可见十字来流段，线宽随 share_pct 变化  
