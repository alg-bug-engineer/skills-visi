# 进口道流量溯源与上游治理落点 Implementation Plan

> **已废弃（2026-07-01）**  
> 本文档中的 **≤2 跳三分叉** 实施步骤已过时。  
> **现行规则：** [`docs/UPSTREAM_TRACE_RULES.md`](../UPSTREAM_TRACE_RULES.md)  
> **现行计划：** [`2026-07-01-流量溯源十字渲染与单进口收口.md`](2026-07-01-流量溯源十字渲染与单进口收口.md)

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** ~~在诊断对话流中新增 `upstream_trace` phase：对过饱和进口道自动溯源上游、递归（≤2 跳）定位可信控治理落点~~ **已实施为单链 ≤5 跳，见 UPSTREAM_TRACE_RULES.md**

**Architecture:** 复用增量扩展。后端 `data_fetcher` 加 `dir8_filter`、`flow_trace_service` 抽 `upstream_one_hop`，新建 `upstream_governance_trace_service` 递归构树并产出 storyboard，`orchestrator` 在 diagnose 后接入 phase。前端 `MapStage` 新增幂等 `renderToFrame`，新增树形侧卡与四向饱和环组件，扩展控制条。

**Tech Stack:** Python 3.12 / asyncpg / pytest（后端）；Vue 3 / TypeScript / 高德 AMap / vitest（前端）。

**设计依据:** `docs/plans/2026-06-30-进口道流量溯源与上游治理落点-设计与计划.md`

**命令约定:**
- 后端测试：`cd backend && python -m pytest <path>::<test> -v`
- 前端测试：`cd frontend-v2 && npx vitest run <path>`

---

## 阶段 B — 后端

### Task B1: data_fetcher 支持按进口道取四向 profile

**Files:**
- Modify: `backend/intersection_agent/services/data_fetcher.py`（`_fetch_by_turn` 加 `dir8_filter`；新增 `_aggregate_approach_profiles`）
- Test: `backend/tests/test_approach_profiles.py`（新建）

**Step 1: Write the failing test**

```python
"""approach_profiles：把 by_turn 聚合成四进口道 profile。"""
from intersection_agent.services.data_fetcher import aggregate_approach_profiles


def test_aggregate_keeps_max_turn_sat_per_dir8():
    by_turn = [
        {"dir8_code": 0, "turn_dir_no": 2, "turn_saturation": 0.95, "green_utilization": 0.38},
        {"dir8_code": 0, "turn_dir_no": 1, "turn_saturation": 0.60, "green_utilization": 0.70},
        {"dir8_code": 4, "turn_dir_no": 2, "turn_saturation": 0.50, "green_utilization": 0.80},
    ]
    profiles = aggregate_approach_profiles(by_turn, by_approach=[])
    north = next(p for p in profiles if p["dir8_code"] == 0)
    assert north["turn_saturation_max"] == 0.95
    assert north["green_util_min"] == 0.38
    assert {p["dir8_code"] for p in profiles} == {0, 4}
```

**Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_approach_profiles.py -v`
Expected: FAIL（ImportError: cannot import name 'aggregate_approach_profiles'）

**Step 3: Write minimal implementation**

在 `data_fetcher.py` 模块级新增（放在文件末尾的 helper 区）：

```python
def aggregate_approach_profiles(
    by_turn: list[dict[str, Any]],
    by_approach: list[dict[str, Any]],
) -> list[dict[str, Any]]:
    """聚合为每进口道一条 profile：取该进口道最大 turn_saturation 与最小 green_util。"""
    queue_by_dir8: dict[int, float] = {}
    for r in by_approach:
        d8 = r.get("dir8_code")
        if d8 is None:
            continue
        q = r.get("queue_len_est_m")
        if q is not None:
            queue_by_dir8[int(d8)] = max(queue_by_dir8.get(int(d8), 0.0), float(q))

    acc: dict[int, dict[str, Any]] = {}
    for r in by_turn:
        d8 = r.get("dir8_code")
        if d8 is None:
            continue
        d8 = int(d8)
        sat = r.get("turn_saturation")
        gu = r.get("green_utilization")
        node = acc.setdefault(
            d8, {"dir8_code": d8, "turn_saturation_max": None, "green_util_min": None}
        )
        if sat is not None:
            node["turn_saturation_max"] = max(node["turn_saturation_max"] or 0.0, float(sat))
        if gu is not None:
            cur = node["green_util_min"]
            node["green_util_min"] = float(gu) if cur is None else min(cur, float(gu))
    for d8, node in acc.items():
        node["queue_len_est_m"] = queue_by_dir8.get(d8)
    return sorted(acc.values(), key=lambda n: n["dir8_code"])
```

> 注：`by_approach` 的 `dir8_code` 需在 `_fetch_by_approach` 的返回里保留（当前只回 `dir8_label`）。同步在 `_fetch_by_approach` 的字典里补 `"dir8_code": r.get("dir8_code")`。

**Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_approach_profiles.py -v`
Expected: PASS

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/data_fetcher.py backend/tests/test_approach_profiles.py
git commit -m "feat(upstream-trace): data_fetcher 聚合四进口道 profile"
```

---

### Task B2: flow_trace 抽 upstream_one_hop 公共函数

**Files:**
- Modify: `backend/intersection_agent/services/flow_trace_service.py`（抽出按 `(inter_id, f_dir8_no)` 锁一跳的纯函数）
- Test: `backend/tests/test_upstream_one_hop.py`（新建）

**Step 1: Write the failing test**

```python
from intersection_agent.services.flow_trace_service import one_hop_for_approach


def test_one_hop_picks_max_coverage_same_direction():
    rows = [
        {"f_dir8_no": 0, "turn_dir_no": 2, "cor_inter_id": "U1", "cor_f_dir8_no": 0,
         "cor_turn_dir_no": 2, "flow_share_ratio": 88.0, "cor_inter_name": "上游A",
         "cor_lng": 117.1, "cor_lat": 36.65},
        {"f_dir8_no": 0, "turn_dir_no": 2, "cor_inter_id": "U2", "cor_f_dir8_no": 0,
         "cor_turn_dir_no": 2, "flow_share_ratio": 40.0, "cor_inter_name": "上游B",
         "cor_lng": 117.2, "cor_lat": 36.66},
    ]
    hop = one_hop_for_approach(rows, dir8=0)
    assert hop is not None
    assert hop["cor_inter_id"] == "U1"
    assert hop["feeding_dir8"] == 0


def test_one_hop_returns_none_when_no_rows_for_dir8():
    assert one_hop_for_approach([], dir8=4) is None
```

**Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_upstream_one_hop.py -v`
Expected: FAIL（ImportError: cannot import name 'one_hop_for_approach'）

**Step 3: Write minimal implementation**

在 `flow_trace_service.py` 新增（复用现有 `lock_one_hop`）：

```python
def one_hop_for_approach(
    rows: list[dict[str, Any]], dir8: int
) -> dict[str, Any] | None:
    """对指定进口道，返回 coverage 最大的一跳上游节点（含 feeding_dir8）。"""
    grouped = lock_one_hop(rows)
    candidates: list[dict[str, Any]] = []
    for (d8, _turn), nodes in grouped.items():
        if d8 == dir8:
            candidates.extend(nodes)
    if not candidates:
        return None
    best = max(candidates, key=lambda n: n["coverage"])
    return {
        "cor_inter_id": best["cor_inter_id"],
        "cor_inter_name": best.get("cor_inter_name"),
        "feeding_dir8": best["cor_dir8"],
        "coverage": best["coverage"],
        "lng": best.get("lng"),
        "lat": best.get("lat"),
    }
```

**Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_upstream_one_hop.py -v`
Expected: PASS

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/flow_trace_service.py backend/tests/test_upstream_one_hop.py
git commit -m "feat(upstream-trace): flow_trace 抽 one_hop_for_approach 公共函数"
```

---

### Task B3: governable 判定（纯函数）

**Files:**
- Create: `backend/intersection_agent/services/upstream_governance_trace_service.py`（先放纯判定函数）
- Test: `backend/tests/test_governable.py`（新建）

**Step 1: Write the failing test**

```python
from intersection_agent.services.upstream_governance_trace_service import is_governable


PROFILES_FULL = [{"dir8_code": d, "turn_saturation_max": 0.95, "green_util_min": 0.6}
                 for d in (0, 2, 4, 6)]
PROFILES_SLACK = [
    {"dir8_code": 0, "turn_saturation_max": 0.95, "green_util_min": 0.6},
    {"dir8_code": 2, "turn_saturation_max": 0.70, "green_util_min": 0.6},  # 未饱和
    {"dir8_code": 4, "turn_saturation_max": 0.95, "green_util_min": 0.6},
    {"dir8_code": 6, "turn_saturation_max": 0.95, "green_util_min": 0.6},
]
PROFILES_EMPTY_GREEN = [
    {"dir8_code": 0, "turn_saturation_max": 0.95, "green_util_min": 0.40},  # 空槽
    {"dir8_code": 2, "turn_saturation_max": 0.95, "green_util_min": 0.6},
    {"dir8_code": 4, "turn_saturation_max": 0.95, "green_util_min": 0.6},
    {"dir8_code": 6, "turn_saturation_max": 0.95, "green_util_min": 0.6},
]


def test_full_saturation_not_governable():
    assert is_governable(PROFILES_FULL, full_sat=0.85, green_util=0.5) is False


def test_has_slack_direction_is_governable():
    assert is_governable(PROFILES_SLACK, full_sat=0.85, green_util=0.5) is True


def test_empty_green_makes_governable():
    assert is_governable(PROFILES_EMPTY_GREEN, full_sat=0.85, green_util=0.5) is True
```

**Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_governable.py -v`
Expected: FAIL（ModuleNotFoundError / ImportError）

**Step 3: Write minimal implementation**

```python
"""上游治理溯源：递归找可信控治理落点（≤2 跳）。"""
from __future__ import annotations
from typing import Any


def is_governable(
    profiles: list[dict[str, Any]], *, full_sat: float, green_util: float
) -> bool:
    """有信控空间 = 非四向全饱和，或任一进口道绿灯利用率有空槽。"""
    if not profiles:
        return False
    has_slack_dir = any((p.get("turn_saturation_max") or 0.0) < full_sat for p in profiles)
    has_empty_green = any(
        p.get("green_util_min") is not None and p["green_util_min"] < green_util
        for p in profiles
    )
    return has_slack_dir or has_empty_green
```

**Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_governable.py -v`
Expected: PASS

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/upstream_governance_trace_service.py backend/tests/test_governable.py
git commit -m "feat(upstream-trace): governable 判定纯函数"
```

---

### Task B4: 递归构树（注入式依赖，纯逻辑可测）

**Files:**
- Modify: `backend/intersection_agent/services/upstream_governance_trace_service.py`（新增 `build_tree`，把"取 profile"和"取上游"作为可注入回调，便于单测不连库）
- Test: `backend/tests/test_build_tree.py`（新建）

**Step 1: Write the failing test**

```python
from intersection_agent.services.upstream_governance_trace_service import build_tree

# 构造确定性拓扑：target(北) → U1(全饱和) → U1的其它3进口各有一个上游，其中A可治理
FAKE_PROFILES = {
    "U1": [{"dir8_code": d, "turn_saturation_max": 0.95, "green_util_min": 0.6} for d in (0, 2, 4, 6)],
    "A":  [{"dir8_code": 0, "turn_saturation_max": 0.70, "green_util_min": 0.6}] +
          [{"dir8_code": d, "turn_saturation_max": 0.95, "green_util_min": 0.6} for d in (2, 4, 6)],
    "B":  [{"dir8_code": d, "turn_saturation_max": 0.95, "green_util_min": 0.6} for d in (0, 2, 4, 6)],
    "C":  [{"dir8_code": d, "turn_saturation_max": 0.95, "green_util_min": 0.6} for d in (0, 2, 4, 6)],
}
FAKE_UP = {
    ("U1", 2): {"cor_inter_id": "A", "feeding_dir8": 2, "cor_inter_name": "A", "coverage": 70, "lng": 1, "lat": 1},
    ("U1", 4): {"cor_inter_id": "B", "feeding_dir8": 4, "cor_inter_name": "B", "coverage": 60, "lng": 1, "lat": 1},
    ("U1", 6): {"cor_inter_id": "C", "feeding_dir8": 6, "cor_inter_name": "C", "coverage": 50, "lng": 1, "lat": 1},
}


def fake_profiles(inter_id, window):
    return FAKE_PROFILES[inter_id]

def fake_upstream(inter_id, dir8, window):
    return FAKE_UP.get((inter_id, dir8))

def fake_approaches(inter_id, exclude):
    return [d for d in (0, 2, 4, 6) if d != exclude]


def test_tree_recurses_to_hop2_and_marks_governance_point():
    node = build_tree(
        "U1", feeding_dir8=0, hop=1,
        window=None, get_profiles=fake_profiles, get_upstream=fake_upstream,
        get_other_approaches=fake_approaches,
        full_sat=0.85, green_util=0.5, max_hops=2,
    )
    assert node["decision"] == "继续上溯"
    children = {c["inter_id"]: c for c in node["children"]}
    assert children["A"]["decision"] == "治理落点"
    assert children["B"]["decision"] == "二跳截止"   # 全饱和但已到 hop2
    assert children["A"]["hop"] == 2


def test_governable_node_stops_immediately():
    node = build_tree(
        "A", feeding_dir8=2, hop=1,
        window=None, get_profiles=fake_profiles, get_upstream=fake_upstream,
        get_other_approaches=fake_approaches,
        full_sat=0.85, green_util=0.5, max_hops=2,
    )
    assert node["decision"] == "治理落点"
    assert node["children"] == []
```

**Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_build_tree.py -v`
Expected: FAIL（ImportError: cannot import name 'build_tree'）

**Step 3: Write minimal implementation**

```python
from collections.abc import Callable


def build_tree(
    inter_id: str,
    *,
    feeding_dir8: int,
    hop: int,
    window: Any,
    get_profiles: Callable[[str, Any], list[dict[str, Any]]],
    get_upstream: Callable[[str, int, Any], dict[str, Any] | None],
    get_other_approaches: Callable[[str, int], list[int]],
    full_sat: float,
    green_util: float,
    max_hops: int,
    inter_name: str | None = None,
) -> dict[str, Any]:
    profiles = get_profiles(inter_id, window)
    governable = is_governable(profiles, full_sat=full_sat, green_util=green_util)
    node: dict[str, Any] = {
        "inter_id": inter_id,
        "inter_name": inter_name,
        "feeding_dir8": feeding_dir8,
        "hop": hop,
        "approach_profiles": profiles,
        "governable": governable,
        "children": [],
    }
    if governable:
        node["decision"] = "治理落点"
        return node
    if hop >= max_hops:
        node["decision"] = "二跳截止"
        return node
    node["decision"] = "继续上溯"
    for ap in get_other_approaches(inter_id, feeding_dir8):
        up = get_upstream(inter_id, ap, window)
        if not up:
            continue
        child = build_tree(
            up["cor_inter_id"],
            feeding_dir8=up["feeding_dir8"],
            hop=hop + 1,
            window=window,
            get_profiles=get_profiles,
            get_upstream=get_upstream,
            get_other_approaches=get_other_approaches,
            full_sat=full_sat,
            green_util=green_util,
            max_hops=max_hops,
            inter_name=up.get("cor_inter_name"),
        )
        node["children"].append(child)
    return node
```

**Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_build_tree.py -v`
Expected: PASS（2 passed）

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/upstream_governance_trace_service.py backend/tests/test_build_tree.py
git commit -m "feat(upstream-trace): 递归构树（注入式依赖，≤2跳，落点判定）"
```

---

### Task B5: storyboard 帧生成（reveal 累计、逐树串讲）

**Files:**
- Modify: `backend/intersection_agent/services/map_presentation_service.py`（新增 `build_upstream_storyboard`）
- Test: `backend/tests/test_upstream_storyboard.py`（新建）

**Step 1: Write the failing test**

```python
from intersection_agent.services.map_presentation_service import build_upstream_storyboard


def _tree(tree_id, approach):
    return {
        "tree_id": tree_id, "approach": approach,
        "root": {
            "inter_id": "U1", "inter_name": "U1", "decision": "继续上溯",
            "hop": 1, "approach_profiles": [], "children": [
                {"inter_id": "A", "inter_name": "A", "decision": "治理落点",
                 "hop": 2, "approach_profiles": [], "children": [],
                 "feeding_dir8": 2, "lng": 1, "lat": 1},
            ], "feeding_dir8": 0, "lng": 1, "lat": 1,
        },
        "target": {"inter_id": "T", "name": "目标", "approach": approach,
                   "lon": 0, "lat": 0, "dir8_code": 0},
    }


def test_frames_are_tree_serialized_and_reveal_is_cumulative():
    sb = build_upstream_storyboard([_tree("N", "北进口"), _tree("E", "东进口")], cognition={})
    # 逐树串讲：所有 N 帧在 E 帧之前
    trees_in_order = [f["tree"] for f in sb["frames"]]
    assert trees_in_order == sorted(trees_in_order, key=lambda t: 0 if t == "N" else 1)
    assert trees_in_order[0] == "N" and trees_in_order[-1] == "E"
    # 第一帧 thesis，reveal 为空
    assert sb["frames"][0]["kind"] == "thesis"
    assert sb["frames"][0]["reveal"] == []
    # 存在 resolve 帧且 focus 含落点
    assert any(f["kind"] == "resolve" for f in sb["frames"])
```

**Step 2: Run test to verify it fails**

Run: `cd backend && python -m pytest tests/test_upstream_storyboard.py -v`
Expected: FAIL（ImportError）

**Step 3: Write minimal implementation**

在 `map_presentation_service.py` 新增 `build_upstream_storyboard(trees, cognition)`：对每棵树生成 `thesis/zoom_out/hop1/inspect/hop2/resolve` 帧序列，串联所有树（保持输入顺序即逐树串讲），`reveal` 为该树内累计 id 列表（edge:/ring:/badge:）。完整实现见设计文档 §3；以"让测试通过"的最小骨架起步，再补 `narration` 文案与 `camera` 字段。

**Step 4: Run test to verify it passes**

Run: `cd backend && python -m pytest tests/test_upstream_storyboard.py -v`
Expected: PASS

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/map_presentation_service.py backend/tests/test_upstream_storyboard.py
git commit -m "feat(upstream-trace): storyboard 帧生成（逐树串讲+reveal累计）"
```

---

### Task B6: Service 装配 + 阈值 + MOCK_DB 端到端

**Files:**
- Modify: `backend/intersection_agent/services/upstream_governance_trace_service.py`（`UpstreamGovernanceTraceService` 类：装配真实 `get_profiles`=DataFetcher、`get_upstream`=查询+`one_hop_for_approach`、`asyncio.gather` 多树）
- Modify: `backend/intersection_agent/rules/thresholds_extensions.yaml`（新增 `upstream_trace` 段）
- Modify: `backend/intersection_agent/services/data_fetcher.py`（`approach_profiles(inter_id, dir8_filter, window)` 异步入口 + MOCK 分支）
- Test: `backend/tests/test_upstream_service_mock.py`（新建，`Settings(mock_db=True)`）

**Step 1–4:** 写一个 `mock_db=True` 下 `await service.build(inter_id, approach="北进口", nlu=...)` 返回 `{"trees": [...], "governance_points": [...], "storyboard": {...}}` 的集成测试；断言至少 1 棵树、storyboard 帧非空、governance_points 含 `decision=="治理落点"` 节点。阈值段：

```yaml
upstream_trace:
  trigger_saturation: 0.90
  full_saturation: 0.85
  governable_green_util: 0.50
  max_hops: 2
```

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/upstream_governance_trace_service.py \
        backend/intersection_agent/services/data_fetcher.py \
        backend/intersection_agent/rules/thresholds_extensions.yaml \
        backend/tests/test_upstream_service_mock.py
git commit -m "feat(upstream-trace): service 装配+阈值+MOCK_DB 集成测试"
```

---

### Task B7: orchestrator 接入 upstream_trace phase

**Files:**
- Modify: `backend/intersection_agent/services/orchestrator.py`（diagnose 成立后、`confirm_bubble` 前：收集过饱和进口道→`UpstreamGovernanceTraceService.build`→`emitter.emit("upstream_trace", running/completed)`+`_emit_map_sequence(action="upstream_tree", data=storyboard)`；confirm 文案改为"已定位 N 个上游治理落点，是否生成跨路口协调建议？"）
- Modify: `backend/intersection_agent/api/schemas.py`（storyboard/tree 响应模型）
- Test: `backend/tests/test_orchestrator_upstream_phase.py`（新建，断言过饱和路口会 emit `upstream_trace` 与 `upstream_tree` action；不过饱和则跳过）

**Step 5: Commit**

```bash
git add backend/intersection_agent/services/orchestrator.py backend/intersection_agent/api/schemas.py backend/tests/test_orchestrator_upstream_phase.py
git commit -m "feat(upstream-trace): orchestrator 接入 phase 与 emit"
```

---

## 阶段 F — 前端

### Task F1: renderToFrame 纯逻辑（reveal 累计 + 当前树高亮）

**Files:**
- Create: `frontend-v2/src/utils/upstreamFrame.ts`（纯函数 `visibleAtFrame(storyboard, n)` 返回 `{overlayIds:Set, activeTree, focus}`）
- Test: `frontend-v2/src/utils/upstreamFrame.spec.ts`（新建）

**Step 1: Write the failing test**

```typescript
import { describe, expect, it } from 'vitest'
import { visibleAtFrame } from './upstreamFrame'

const SB = {
  trees: [], frames: [
    { idx: 0, tree: 'N', kind: 'thesis', focus: 'target', reveal: [] },
    { idx: 1, tree: 'N', kind: 'hop1', focus: 'up1', reveal: ['edge:N-up1'] },
    { idx: 2, tree: 'N', kind: 'inspect', focus: 'up1', reveal: ['ring:up1'] },
    { idx: 3, tree: 'E', kind: 'thesis', focus: 'target', reveal: [] },
    { idx: 4, tree: 'E', kind: 'hop1', focus: 'up1', reveal: ['edge:E-up1'] },
  ],
} as any

describe('visibleAtFrame', () => {
  it('accumulates reveal ids up to n', () => {
    const v = visibleAtFrame(SB, 2)
    expect([...v.overlayIds].sort()).toEqual(['edge:N-up1', 'ring:up1'])
    expect(v.activeTree).toBe('N')
  })
  it('rewinding drops later overlays (frame reconstruction)', () => {
    expect([...visibleAtFrame(SB, 1).overlayIds]).toEqual(['edge:N-up1'])
    expect([...visibleAtFrame(SB, 0).overlayIds]).toEqual([])
  })
  it('switches active tree at handoff', () => {
    expect(visibleAtFrame(SB, 4).activeTree).toBe('E')
  })
})
```

**Step 2: Run test to verify it fails**

Run: `cd frontend-v2 && npx vitest run src/utils/upstreamFrame.spec.ts`
Expected: FAIL（Cannot find module './upstreamFrame'）

**Step 3: Write minimal implementation**

```typescript
export interface UpstreamFrame { idx: number; tree: string; kind: string; focus: unknown; reveal: string[]; camera?: string; narration?: string }
export interface UpstreamStoryboard { trees: unknown[]; frames: UpstreamFrame[] }

export function visibleAtFrame(sb: UpstreamStoryboard, n: number) {
  const overlayIds = new Set<string>()
  const clamped = Math.max(0, Math.min(n, sb.frames.length - 1))
  for (let i = 0; i <= clamped; i++) for (const id of sb.frames[i].reveal) overlayIds.add(id)
  const frame = sb.frames[clamped]
  return { overlayIds, activeTree: frame.tree, focus: frame.focus, frame }
}
```

**Step 4: Run test to verify it passes**

Run: `cd frontend-v2 && npx vitest run src/utils/upstreamFrame.spec.ts`
Expected: PASS（3 passed）

**Step 5: Commit**

```bash
git add frontend-v2/src/utils/upstreamFrame.ts frontend-v2/src/utils/upstreamFrame.spec.ts
git commit -m "feat(upstream-trace): renderToFrame 纯逻辑（帧重建累计）"
```

---

### Task F2: DirSaturationRing 四向饱和环组件

**Files:**
- Create: `frontend-v2/src/components/insight/DirSaturationRing.vue`
- Create: `frontend-v2/src/utils/ringSeverity.ts`（`severityColor(sat)` 纯函数，复刻后端 `_severity` 阈值）
- Test: `frontend-v2/src/utils/ringSeverity.spec.ts`

**Step 1–4:** 先 TDD `severityColor`（如 `>=0.9→红, >=0.75→橙, 否则绿`，对齐后端口径），再用它驱动 `DirSaturationRing.vue` 的 4 段 SVG 弧。组件测试用 `@vue/test-utils` 断言渲染 4 段并按 props 着色。

**Step 5: Commit**

```bash
git add frontend-v2/src/components/insight/DirSaturationRing.vue frontend-v2/src/utils/ringSeverity.ts frontend-v2/src/utils/ringSeverity.spec.ts
git commit -m "feat(upstream-trace): 四向饱和环组件"
```

---

### Task F3: MapStage 接入 upstream_tree（运镜 + 本地播放）

**Files:**
- Modify: `frontend-v2/src/components/MapStage.vue`（`case 'upstream_tree'`：缓存 storyboard、`renderUpstreamFrame(n)` 调用 `visibleAtFrame` 重绘 Polyline/环/badge、本地定时器自动播放、`setFitView` 按 camera、用户介入暂停）
- Modify: `frontend-v2/src/types/map.ts`（`UpstreamStoryboard` 等类型、`MapActionEvent` 加字段）

> 渲染副作用层不强制 TDD；逻辑都已在 F1 的 `visibleAtFrame` 覆盖。手动验证：`cd frontend-v2 && npm run dev`，触发过饱和路口诊断，观察逐树串讲与拖回重建。

**Step 5: Commit**

```bash
git add frontend-v2/src/components/MapStage.vue frontend-v2/src/types/map.ts
git commit -m "feat(upstream-trace): MapStage 接入 upstream_tree 运镜与本地播放"
```

---

### Task F4: UpstreamGovernanceCard 树形侧卡 + 控制条 + 联动

**Files:**
- Create: `frontend-v2/src/components/insight/UpstreamGovernanceCard.vue`（方向 Tab、树形列表、二跳折叠、落点建议、点击联动 emit）
- Create: `frontend-v2/src/components/insight/UpstreamControlBar.vue`（播放/暂停/单步/刮擦/倍速/展开二跳，复用现有节奏控件样式）
- Modify: 父级页面挂载卡片（左下角，对齐既有卡位约定）
- Test: `frontend-v2/src/components/insight/UpstreamGovernanceCard.spec.ts`（断言 Tab 渲染、二跳折叠、落点★标记、点击 emit `focus-node`）

**Step 5: Commit**

```bash
git add frontend-v2/src/components/insight/UpstreamGovernanceCard.vue frontend-v2/src/components/insight/UpstreamControlBar.vue frontend-v2/src/components/insight/UpstreamGovernanceCard.spec.ts
git commit -m "feat(upstream-trace): 治理落点树形侧卡+控制条+联动"
```

---

## 阶段 V — 端到端验证

### Task V1: 全链路联调

1. 后端全测：`cd backend && python -m pytest -q`，Expected: all pass。
2. 前端全测：`cd frontend-v2 && npx vitest run`，Expected: all pass。
3. 手动 E2E（参考 `superpowers:verification-before-completion`）：
   - 触发"奥体西路与经十路交叉口 北进口 早高峰"诊断；
   - 验证步骤条出现「流量溯源」、地图缩放到道路级、逐树串讲、四向环、落点高亮；
   - 控制条暂停/单步/拖回重建、展开二跳、多方向 Tab 切换；
   - 收束后 confirm 文案为"已定位 N 个上游治理落点…"。
4. Commit 复盘：在 `docs/plans/2026-06-30-进口道流量溯源与上游治理落点-复盘.md` 记录实测与偏差。

---

## 执行注意

- **DRY/YAGNI**：边 path 缺失才退直线，不做中心飞线；`other_three_approaches` 按实际进口道集合（非固定 4）。
- **TDD**：每个纯函数先红后绿；Vue 渲染层把可测逻辑下沉到 utils。
- **频繁提交**：每 Task 一提交，分支 `feat/upstream-governance-trace`。
- **容错**：溯源失败仅告警不阻断主诊断。
