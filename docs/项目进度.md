# 交通智能体项目进度

> 最后更新：2026-07-08  
> 测试基线：`.venv/bin/python -m pytest tests/ -q` → **145 passed**；`npm run test`（frontend）→ **102 passed**；`npm run build` 通过。需求15 live fixture 重采阻塞于 `QWEN_API_KEY` 未配置。
> 项目组织优化（v3.0）：精简被宏观测试包含的微观单测（后端 -9 文件、前端 -8 文件，宏观/集成/e2e 覆盖不变）；移除死组件 `InsightPanel.vue`/`PhaseDiagram.vue` 与调试产物 `flow-trace-links-sniff.html`；已合并分支清理。

## 需求总览

| 需求 | 主题 | 进度 | 说明 |
|------|------|------|------|
| 需求 1 | 初始化后端 | **100%** | 5 Skill 流水线、Runtime、API、mock LLM 端到端 |
| 需求 2 | 生产化优化 | **95%** | 数据真源、护栏、剧本字段；`evaluation-feedback` Skill 为非目标 |
| 需求 3 | 经验沉淀 | **100%** | NLU 三类经验、方案反馈扩展、成因/方案检索引用 |
| 需求 4 | 前端就绪与剧本补齐 | **100%** | 分步 API、PG 加载、字段补齐、集成文档 |
| 需求 5 | 前端 UI 九幕演示 | **100%** | Vue3+TS+Vite 独立工程 SPA 已交付 |
| 需求 6 | 流式渐进演出 | **100%** | SSE 边算边渲染（路径B） |
| 需求 7 | 流量溯源拓扑可视化 | **100%** | 后端补全真实几何，禁止前端合成 |
| 需求 8 | 流量溯源前端可视化与镜头连贯 | **100%** | 发光路径 + 连贯运镜（去除流动粒子） |
| 需求 9 | 标准流量溯源拓扑与渠化可视化 | **100%** | `channelization_map` + 渠化/下游拓扑图层 |
| 需求 10 | 闭环演示呈现优化 | **前端 90% / 后端待办** | 证据门控 + 产品化文案 + 下游拓扑指标；配时/协调图数据待后端补齐（`needs/10`） |
| 需求 13 | 闭环 UI 优化与运行数据/案例库增强 | **100%** | R1 诊断工单无序列表；R2 行业/路口案例库（expert_knowledge 构建期 JSON）；R3 运行数据富指标卡；R4 问题验证卡 + 治理策略卡 + 参考依据点击定位 |
| 需求 14 | 方案下发后经验吸收/技能固化可视化 | **100%** | 下发后技能固化弹窗；不固化返主页；固化走经验吸收(6阶段)+技能固化(7阶段)可视化并真实落盘技能包；完成态技能卡+下载+返回主页 |
| 需求 15 | 方案生成生产级证据面板前后端联动 | **代码完成 / live 重采阻塞** | 后端输出现状/优化/方向/强度证据契约，前端证据面板已接入；需配置 `QWEN_API_KEY` 后重采 fixture |
| 需求 16 | 闭环 UI 布局与中文文案优化 | **100%** | 运行数据左下详情 + 闭环内摘要；侧栏与治理建议通顶同高；经验固化并入右侧 Tab；枚举中文化；退回修改回主页 |
| 需求 17 | 演示节奏/闭环一致性/产品定位 | **100%** | R1 组合节奏（打字 28ms + 第2幕起停留 2s，可环境变量配置）；R2 旁白缓存消除打字/展开文案分叉；R3 产品壳层改「交通信控」（排队溢出仅作示例场景） |
| 需求 18 | 闭环 UI 持续优化 | **100%** | 主页经验库悬浮抽屉（露边+悬停展开）；去除经验库/案例库数字角标；下游承接判别新增本路口指标块与结论溯源（消除「指标为零却判利用不足」误导）；下游拓扑图、治理最小化、饱和度小数、删除问题验证卡/策略包、相似案例仅编号可点、运行数据三行列表、技能固化左抽屉 |
| 需求 19 | 无问题路口健康分支 | **100%** | 后端 `analyze_overflow` 产出 `healthy` 标志、`executor` 诊断健康即提前正常收尾（跳过成因/策略/方案）；前端 `isHealthy` 门控在「溢出证据核验」幕收尾，新增 `HealthyConclusionCard`（诊断/策略/处置文案）与健康演示 fixture/入口 |
| 需求 20 | 方案与协调呈现优化及下游指标修复 | **100%** | R1 方案 movement/供需强度中文化（后端补 `label`，杜绝 `d2_t2`）；R2 供需强度/虚拟流量口径说明；R3 删除「优化审计」面板；R4 干线协调图增加解释、来源/周期中文化；R5 红线补齐量化约束（最小/最大绿、最大周期）；R6 修复 PG 下游承接指标恒为 0/无（`BUG-004`：拓扑补载相邻路口真实指标，缺失降级不伪造 0） |

## 剧本九幕实现状态

| 幕 | 主题 | 后端状态 | 前端对接文档 |
|----|------|----------|--------------|
| 一 | 诊断工单 | ✅ done | `diagnosis_ticket` |
| 二 | 落到路网 | ⚠️ partial | `spatial_scene` + `POST /intersection/load` |
| 三 | 溢出验证 | ⚠️ partial | `metrics` + `scenario_report` |
| 四 | 本路口 vs 下游 | ✅ done | `phases.diagnosis.downstream_diagnosis` |
| 五 | 干线协调 | ⚠️ partial | `phases.diagnosis.arterial_analysis` |
| 六 | 成因 + 案例 | ⚠️ partial | `phases.cause` |
| 七 | 策略生成 | ✅ done | `strategy.recommended` 等已透出 |
| 八 | 方案生成 | ✅ done | `plan.recommendation` 已透出 |
| 九 | 反馈沉淀 | ⚠️ partial | decision + regenerate + `GET /agent/cases` |

## references/intersection 对齐状态

| references Skill | 当前 skills | 对齐度 |
|------------------|-------------|--------|
| scene-cognition | data-analysis-diagnosis | 中（`scenario_report` 已接入） |
| problem-diagnosis | cause-analysis | 中（检查单 gaps 并入 `data_gaps`） |
| control-strategy | strategy-generation | 中（3 溢出包 vs 8 生产包） |
| plan-generation | plan-generation | 高（真实流量绑定 + 单点优化器 + 退化护栏，配时对齐参考项目·需求12） |
| evaluation-feedback | 无 | 未实现（非目标） |

## 数据真源与演示路径

| 配置 | 生产默认 | 测试默认（conftest） |
|------|----------|---------------------|
| `ALLOW_DEMO_FALLBACK` | `false` | `true` |
| `LLM_MOCK` | `false` | `true` |
| 路口注册表 `intersection_registry.json` | **禁用**（需 `PG_DSN` 或 `task` 注入） | 启用 |
| 诊断 metrics/topology | PG 或 `task` 注入 | fixture 或注入 |
| 现状配时 | `task.signal` 或 PG | fixture（`allow_demo_fallback`） |

生产环境须配置 `PG_DSN`，或通过 `POST /api/v1/agent/run` 的 `task` 字段注入 `metrics`、`topology`、`signal`。

## 文档索引

| 文档 | 用途 |
|------|------|
| `docs/剧本.md` | 叙事验收标准 |
| `docs/剧本字段-API对照.md` | 前端对接字段路径 |
| `docs/rule.md` | 开发约束 |
| `needs/1`～`needs/4` | 需求说明 |
| `plans/1`～`plans/4` | 开发计划 |
| `docs/前端集成指南.md` | 前端联调步骤与分步契约 |
| `bugs/` | 已知问题与修复记录 |

## 待办

> P0/P1 已并入需求11（分支 `20260707205219-11-…`）：见 `needs/11`、`plans/11`。

### P0 · 需求10 后端补齐（阻塞治理建议真实呈现）
1. 配时方案明细：`cycle_s` + `phase_stage_timing_list` 链路完整、真实 PG 来源，缺失即 `available/reason` 降级。**已补测试**。
2. 干线协调图数据：新增 `diagnosis.coordination`（上下游节点、间距、绝对相位/相位差、行程速度/旅行时间、协调方向、来源），占位 `phase_offset_match="pg"` 已替换为真实相位关系或降级值。**已完成**（`app/trace/coordination.py` + 单测）。
3. 数据可用性标识：协调块缺字段返回 `available=false` + `reason`（缺节点间距/绝对相位/路段速度）。**已完成**。

### P1 · 前端技术债清理
4. 移除 `store.planTab` / `setPlanTab` / `PlanTab` 与 `frontend/src/viz/TimeSpaceDiagram.vue`；新增仅消费后端真实字段的 `panels/CoordinationDiagram.vue`。**已完成**。
5. 重采 `frontend/src/mock/run_1_fixture.json`（带真实 `channelization_map`/相邻指标/`coordination`）：**阻塞于 PG 环境**——本地无 PG 且唯一含渠化的日志 `pipeline_complete=false`，需真实 PG 运行后 `scripts/capture_frontend_mock.py --live` 重采；在此之前 mock 中 `coordination` 缺省，前端降级为「暂不绘制协调图」。

### 需求12 · 方案生成配时优化对齐参考项目（已完成）
- 新增 `app/data/turn_flow_binding.py`：真实逐转向流量/饱和度/车道绑定到相位（多转向展开、缺数据 `flow_binding.reason` 降级）。
- `request_builder` 去占位、下发 `currentTiming`/`greenBounds`/`min_green_s`，修复引擎回落 14s 最小绿。
- 优化输出回填逐相位饱和度 + `meta`；退化护栏 `_degradation_reason` 回退真实现状配时、不冒充优化结果。
- 顶层 `recommended_plan_id` 补齐。真实 PG 端到端配时 cycle≈98、绿 36/12/35、饱和度 0.75~0.87（详见 `bugs/BUG-003`）。

### 需求15 · 方案生成生产级证据面板前后端联动（代码完成，待 live 重采）
- 后端 `run_single_point_optimizer` 新增 `timing` 证据契约：`current_cycle_s`、`cycle_delta_s`、阶段 `current_timing/optimized_timing`、`movements`、`meta.data_quality`。
- `request_builder` 保留 `stageTotalSec` 与 movement 证据字段（`movementKey/label/saturation/source/flow_available`），禁止前端从阶段名猜方向。
- 优化器退化 fallback 标记 `timing.available=false` + `reason/missing_fields`，避免把可运行配时误展示为生产级证据。
- `scripts/capture_frontend_mock.py --live` 改为失败关闭：`LLM_MOCK=true` 或方案证据字段不完整时拒绝写入 fixture。
- 前端新增 `PlanEvidencePanel`、`StageCards`、`StageMovementCanvas`、`DirectionIntensityPanel`、`OptimizationAuditPanel`，并接入 `PlanDrawer`。
- 验证：`pytest tests/test_plan_evidence_contract.py tests/test_request_builder.py tests/test_response_contract.py tests/test_plan_optimizer_degradation.py tests/test_capture_frontend_mock.py` 通过；`npm run test` 通过；`npm run build` 通过。
- 阻塞：当前环境未配置 `QWEN_API_KEY`，`scripts/capture_frontend_mock.py --live` 在意图理解阶段失败，未覆盖 `frontend/src/mock/run_1_fixture.json`。配置真实 LLM/PG 后需重跑 live 重采，再执行 `VITE_MOCK=1` 前端 e2e。

### P2 · 长期
6. `evaluation-feedback` Skill（KPI 对比 + 迭代路由）— 单独排期。

### 需求13 · 闭环 UI 优化与运行数据/案例库增强（已完成）
- **R1** 诊断工单卡改无序列表，补齐原始问题/诊断范围等 NLU 字段。
- **R2** `expert_knowledge.md` 构建期解析为 `expertKnowledge.json`（19 场景）；案例库重构为行业案例/路口案例子 tab + 搜索 + 结构化展示 + 锚点。
- **R3** 后端透传逐进口/逐转向富指标 + 配时画像；前端运行数据卡详细面板（含缺失降级）。
- **R4** 问题常发性/周期性派生（标注「派生」）；治理策略参考依据字段；问题验证卡 + 治理策略卡（`extraCards` 接入闭环）；参考依据 chip 点击定位案例库并高亮。

### 需求14 · 方案下发后经验吸收/技能固化可视化（已完成）
- 后端：新增 `app/services/skill_solidification_service.py`，把已采纳的真实 `诊断/策略/方案` 派生并**真实落盘**为 Agent-Skill 包（`SKILL.md`+`reference.md`+`scripts/fetch_traffic_data.sql`+`skill.meta.json`），按目录键 sha256 签名 upsert（created/updated/unchanged）；路由 `POST /agent/skill/solidify`、`GET /agent/skills/solidified`、`GET /agent/skills/{id}/download`（zip，含路径穿越校验）；产物目录 `data/skills/` 已 gitignore。
- 前端：`accept()` 下发成功后不再直接回主页，弹出「技能固化」确认；「暂不固化」返回主页；「确认固化」按 `recap→…→blueprint` 6 阶段经验吸收 + `understanding→…→packaging` 7 阶段固化可视化（阶段时间线/文件树/代码逐字/进度）；完成态技能卡 + 下载技能包 + 返回主页。所有可视化数据均来自后端真实返回，缺失降级不编造。
- 架构决策：采用「单次 solidify 返回结构化真实结果 + 前端分阶段动画」，不引入第二套 SSE 基建（详见 `needs/14`/`plans/14`）。

### 需求20 · 方案与协调呈现优化及下游指标修复（已完成）
- **R1** `run_single_point_optimizer` 对 movement 证据与 `direction_intensity_list` 统一补齐人可读中文 `label`（引擎转向码口径：0=掉头/1=左/2=直/3=右），前端不再回落 `d2_t2`；同步补齐 `frontend/src/mock/run_1_fixture.json` 56 处 movement 标签。
- **R2** `DirectionIntensityPanel` 增加 I_dir/I_obj 口径说明与「虚拟流量」解释（历史兜底流量，仅保底最小绿，非实测需求）。
- **R3** 删除「治理建议」中的「优化审计」面板（移除 `OptimizationAuditPanel.vue`，`PlanEvidencePanel` 降级态改列 `missing_fields`）。
- **R4** `CoordinationDiagram` 增加时距图解释、`来源`/`周期` 中文化（link_geom→路段几何、pg_signal→信号配时、link_speed→路段车速；周期→公共信号周期）。
- **R5** `build_strategy_profile` 从真实 `signal`/`constraints` 派生量化红线（最小绿/最大绿/最大周期），追加到 `hard_constraints` 并输出 `quantitative_constraints`；无真实值不编造，标记 `missing`。
- **R6** 修复 `BUG-004`：`enrich_downstream_metrics` 为 PG 下游节点按承接方向补载真实指标；`intersection_load_service`/`load_pg_bundle` 接线；`build_intersection_profile` 尊重 `metrics_available=false`，缺失降级为 `None`（不伪造 0）。
- 验证：新增 `tests/test_plan_movement_labels.py`、`tests/test_strategy_hard_constraints.py`、`tests/test_downstream_metrics_enrichment.py`；后端 `145 passed`，前端 `102 passed` + `npm run build` 通过。

### 需求21 · 运行数据呈现与协调图可读性优化（已完成，同分支续作）
- **R1** 左上角地图 HUD（`mapMarkers.buildHudMetrics`）：饱和度/绿灯利用率由百分比改小数；新增「方向失衡」「进口道长度（＝排队比分母 `storage_length_m`）」；`DataMetricsCard` detail 变体移除「方向失衡」行（失衡改由左上 HUD 呈现）。
- **R2** 取消右下角「渠化·车道级」小窗：`App.vue` 移除 `ChannelizationInset` 挂载/import/样式并删除组件（地图渠化图层不受影响）。
- **R3** 治理策略幕（`act7_strategy`）镜头 `zoom` 17→18（+1 放大）。
- **R4** `CoordinationDiagram` 重构为标准「时距图」：纵轴累计间距（逐路口参考线+名称+累计 m）、横轴一个周期内相位时间（0/¼/½/¾/满 刻度秒标注）、相位落点+协调折线、轴标题与解读文案；保留真实数据消费与 `.coord__nodes`/降级契约。
- 验证：新增 `frontend/tests/unit/map-hud-metrics.test.ts`；更新 `data-metrics-card.test.ts`（失衡移出明细面板断言）；前端 `105 passed` + `npm run build` 通过。

### 需求22 · 约束展示修复、首步耗时优化与溯源/数据缺失排查（已完成，同分支续作）
- **R1（约束拆字修复）** `DiagnosisTicketCard` 的 `约束` 与 `GovernanceStrategyCard`/`StrategyBoundaryCard` 的 `红线` 统一改用规范化计算属性：约束若为字符串（NLU 常直接返回如「优先避免下游继续外溢」）整体成一条标签，不再被 `v-for` 逐字拆成「优/先/避/免…」。
- **R4（首步耗时排查+修复，实测 18.9s→4.4s）** 根因：`pg_client.read_pg_rows` **每条查询新建一次远端 PG 连接**（实测握手 ~290ms/条），整份检查单约 20 条 → 数秒纯连接开销；叠加需求20 下游富化按节点各跑一份**完整检查单**（含 AOI/POI 空间连接等最慢查询）。修复：①新增上下文级连接复用 `pg_client.pg_connection()`（contextvar 按请求隔离，autocommit），`iter_checklist_load`/`load_intersection_metrics_only` 全程复用单连接；②新增轻量指标加载 `load_intersection_metrics_only`（仅跑 `_aggregate_metrics` 所需 9 条指标查询，跳过 AOI/几何/信号），下游富化改走该轻量路径。实测目标路口加载 18.94s→4.36s，相邻节点 full 12.55s→2.74s、lite 3.54s→1.01s，指标数值与全量加载一致。
- **R3（当前路口数据缺失排查）** 结论为**真实 PG 覆盖缺口**而非回归：目标「奥体西路与经十路路口」在该时段 `turn_perf`（排队/延误/停车）查询 **0 行**，而 `turn_saturation/turn_flow/green_utilization` 各 40 行齐全 → HUD 排队/延误为 0、饱和度却高。系统未伪造数据（符合 rule 14/16）；`metrics_for_diagnosis` 逻辑未改动。
- **R2（流量溯源逻辑排查）** 结论为**本分支未改动溯源逻辑**：`app/trace/flow_trace.py`、`app/trace/map_scene.py` 末次变更为需求15（本次仅改 `intersection_profile.py`）；`_coverage_index` 覆盖率语义与参考归档 `references/流量溯源/README.md` 一致（西进口经十路 `DOWNSTREAM→来向 / UPSTREAM→去向` 反转口径保持）；`flow_correlate` 实测 3327 行、`trace_geometry` 10 行，溯源为真实数据驱动。差异源于路口/时段/呈现粒度（走廊聚合 vs 单跳 link），非逻辑改动。
- 验证：新增 `frontend/tests/unit/diagnosis-ticket-card.test.ts` 字符串约束用例；前端 `106 passed` + `npm run build` 通过；后端 `145 passed`。

当前开发分支：`20260708170725-20-方案与协调呈现优化及下游指标修复`（待合并；已合并需求13~19；含需求20、21、22 续作；v3.0 完成项目组织优化与测试/代码精简）
