# 交通智能体项目进度

> 最后更新：2026-07-07  
> 测试基线：`.venv/bin/python -m pytest tests/ -q` → **97 passed**；`npm run test -- --run`（frontend）→ **60 passed**；`npm run build` 通过

## 需求总览

| 需求 | 主题 | 进度 | 说明 |
|------|------|------|------|
| 需求 1 | 初始化后端 | **100%** | 5 Skill 流水线、Runtime、API、mock LLM 端到端 |
| 需求 2 | 生产化优化 | **95%** | 数据真源、护栏、剧本字段；`evaluation-feedback` Skill 为非目标 |
| 需求 3 | 经验沉淀 | **100%** | NLU 三类经验、方案反馈扩展、成因/方案检索引用 |
| 需求 4 | 前端就绪与剧本补齐 | **100%** | 分步 API、PG 加载、字段补齐、集成文档 |
| 需求 5 | 前端 UI 九幕演示 | **100%** | Vue3+TS+Vite 独立工程 SPA 已交付 |
| 需求 6 | 流式渐进演出 | **100%** | SSE 边算边渲染（路径B） |
| 需求 7 | 流量溯源拓扑可视化 | **100%** | 后端补全真实几何，禁止前端合成 |
| 需求 8 | 流量溯源前端可视化与镜头连贯 | **100%** | 发光路径 + 连贯运镜（去除流动粒子） |
| 需求 9 | 标准流量溯源拓扑与渠化可视化 | **100%** | `channelization_map` + 渠化/下游拓扑图层 |
| 需求 10 | 闭环演示呈现优化 | **前端 90% / 后端待办** | 证据门控 + 产品化文案 + 下游拓扑指标；配时/协调图数据待后端补齐（`needs/10`） |

## 剧本九幕实现状态

| 幕 | 主题 | 后端状态 | 前端对接文档 |
|----|------|----------|--------------|
| 一 | 诊断工单 | ✅ done | `diagnosis_ticket` |
| 二 | 落到路网 | ⚠️ partial | `spatial_scene` + `POST /intersection/load` |
| 三 | 溢出验证 | ⚠️ partial | `metrics` + `scenario_report` |
| 四 | 本路口 vs 下游 | ✅ done | `phases.diagnosis.downstream_diagnosis` |
| 五 | 干线协调 | ⚠️ partial | `phases.diagnosis.arterial_analysis` |
| 六 | 成因 + 案例 | ⚠️ partial | `phases.cause` |
| 七 | 策略生成 | ✅ done | `strategy.recommended` 等已透出 |
| 八 | 方案生成 | ✅ done | `plan.recommendation` 已透出 |
| 九 | 反馈沉淀 | ⚠️ partial | decision + regenerate + `GET /agent/cases` |

## references/intersection 对齐状态

| references Skill | 当前 skills | 对齐度 |
|------------------|-------------|--------|
| scene-cognition | data-analysis-diagnosis | 中（`scenario_report` 已接入） |
| problem-diagnosis | cause-analysis | 中（检查单 gaps 并入 `data_gaps`） |
| control-strategy | strategy-generation | 中（3 溢出包 vs 8 生产包） |
| plan-generation | plan-generation | 高（优化器 + 护栏已接入） |
| evaluation-feedback | 无 | 未实现（非目标） |

## 数据真源与演示路径

| 配置 | 生产默认 | 测试默认（conftest） |
|------|----------|---------------------|
| `ALLOW_DEMO_FALLBACK` | `false` | `true` |
| `LLM_MOCK` | `false` | `true` |
| 路口注册表 `intersection_registry.json` | **禁用**（需 `PG_DSN` 或 `task` 注入） | 启用 |
| 诊断 metrics/topology | PG 或 `task` 注入 | fixture 或注入 |
| 现状配时 | `task.signal` 或 PG | fixture（`allow_demo_fallback`） |

生产环境须配置 `PG_DSN`，或通过 `POST /api/v1/agent/run` 的 `task` 字段注入 `metrics`、`topology`、`signal`。

## 文档索引

| 文档 | 用途 |
|------|------|
| `docs/剧本.md` | 叙事验收标准 |
| `docs/剧本字段-API对照.md` | 前端对接字段路径 |
| `docs/rule.md` | 开发约束 |
| `needs/1`～`needs/4` | 需求说明 |
| `plans/1`～`plans/4` | 开发计划 |
| `docs/前端集成指南.md` | 前端联调步骤与分步契约 |
| `bugs/` | 已知问题与修复记录 |

## 待办

> P0/P1 已并入需求11（分支 `20260707205219-11-…`）：见 `needs/11`、`plans/11`。

### P0 · 需求10 后端补齐（阻塞治理建议真实呈现）
1. 配时方案明细：`cycle_s` + `phase_stage_timing_list` 链路完整、真实 PG 来源，缺失即 `available/reason` 降级。**已补测试**。
2. 干线协调图数据：新增 `diagnosis.coordination`（上下游节点、间距、绝对相位/相位差、行程速度/旅行时间、协调方向、来源），占位 `phase_offset_match="pg"` 已替换为真实相位关系或降级值。**已完成**（`app/trace/coordination.py` + 单测）。
3. 数据可用性标识：协调块缺字段返回 `available=false` + `reason`（缺节点间距/绝对相位/路段速度）。**已完成**。

### P1 · 前端技术债清理
4. 移除 `store.planTab` / `setPlanTab` / `PlanTab` 与 `frontend/src/viz/TimeSpaceDiagram.vue`；新增仅消费后端真实字段的 `panels/CoordinationDiagram.vue`。**已完成**。
5. 重采 `frontend/src/mock/run_1_fixture.json`（带真实 `channelization_map`/相邻指标/`coordination`）：**阻塞于 PG 环境**——本地无 PG 且唯一含渠化的日志 `pipeline_complete=false`，需真实 PG 运行后 `scripts/capture_frontend_mock.py --live` 重采；在此之前 mock 中 `coordination` 缺省，前端降级为「暂不绘制协调图」。

### P2 · 长期
6. `evaluation-feedback` Skill（KPI 对比 + 迭代路由）— 单独排期。

当前开发分支：`20260707205219-11-治理建议配时协调后端补齐与前端技术债清理`
