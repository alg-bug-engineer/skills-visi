# 计划 27 · 语音认知、地图动作、经验对比与案例相似度

> 需求：`needs/27-语音地图经验案例迭代.md`  
> 分支：`20260710101225-27-语音地图经验案例迭代`

## 范围（本分支）

| 条目 | 状态 | 说明 |
|------|------|------|
| **R1** 路口认知语音 | ✅ | `axis_roads` 真源 + act2 TTS |
| **R4** 案例相似度细化 | ✅ | 后端契约 + CauseCard UI |
| **R2** 全链路地图动作 | ✅ | act3–8 分层 evidence + map_scenes  enrichment |
| **R3** 有经验 vs 无经验 | ✅ | `experience_contrast` + 吸收/策略卡对照 |
| **R5** 轨迹对接亚楠 | ❌ 忽略 | 确认无轨迹数据，不开发 |

## R2 设计

1. `app/trace/act_map_enrichment.py`：`diagnosis_compare` / `cause_spatial` / `plan_preview` 写入 `diagnosis.map_scenes`。
2. `response_builder.enrich_map_scenes` 在公开快照阶段统一 enrichment。
3. `sceneEvidencePolicy` 扩展 `diagnosisCompare` / `causeAnnotation` / `planPreview`；act6 evidence 改为 `cause_annotation`。
4. `MapController` 新增 `drawDiagnosisCompare` / `drawCauseAnnotations` / `drawPlanPreview`；`control_scope_map.coordination_paths` 绘制上游协调折线。
5. `prefers-reduced-motion`：成因幕连线静态、无粒子。

## R3 设计

1. `build_experience_contrast.py`：策略/成因/护栏/下游四维度对照，refs 可追溯 `record_id` / `case_id`。
2. 策略 Skill 输出 `phases.strategy.experience_contrast`；固化 `build_absorption` 写入 `value_snapshot.contrast_items`。
3. `GovernanceStrategyCard` + `ExperienceAbsorptionPanel` 呈现对照。

## 测试

```bash
PYTHONPATH=. .venv/bin/python -m pytest tests/test_act_map_enrichment.py tests/test_experience_contrast.py tests/test_axis_roads.py -q
cd frontend && npm test -- presentation-closure governance-strategy-contrast voice-spatial-cognition cause-card-cases
```
