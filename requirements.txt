# 交通路口智能体 — 生产运行时依赖
# 安装: pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
dashscope>=1.25.11

# PostgreSQL 数据真源
psycopg[binary]>=3.2.0

# 流量溯源路段覆盖（需求 33）：DuckDB 读恢复轨迹 parquet
duckdb>=1.1.0
pandas>=2.2.0

# signal_optimization_engine 单点优化器
numpy>=2.0.0
scipy>=1.14.0
